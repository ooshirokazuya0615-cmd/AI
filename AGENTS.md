# このリポジトリでの作業ルール（Codex / その他のAIエージェント向け）

Claude Code 向けの `CLAUDE.md` と同じ内容です。
どのツールから頼まれても、同じ文体・同じ型で書くために置いています。
ルールを変えるときは `CLAUDE.md` と `AGENTS.md` の両方を直してください。

## 大前提

ここはぱっちゃん（EST）の発信物を作る場所です。
**日本語の発信用文章を書くときは、必ず `style/voice.md` を読み、その文体で書くこと。**

対象：メルマガ、ステップメール、LINE配信、セールスレター、ブログ記事、
YouTube朗読シナリオ（スカッとする話・感動する話）、読者への呼びかけ文。

「僕の文体で」と言われなくても、発信用の文章を頼まれた時点で適用します。

## 書くときの手順

1. `style/voice.md` を読む
2. `templates/` から該当する型を読む（メルマガなら `templates/melmaga.md`）
3. 迷ったら `style/samples/` の過去原稿を実例として参照する
4. 書き上げたら `style/voice.md` 末尾のチェックリストで自己点検する
5. 成果物は `drafts/` に置く。ファイル名は `YYYY-MM-DD-種別-テーマ.md`

## 絶対に守ること

- **長い段落を作らない。** この文体の生命線は改行と空白行の縦のリズム。
- 硬い書き言葉（〜である、〜と考えられる、〜すべきだ）を使わない。
- 数字・固有名詞・セリフを必ず入れる。抽象的なきれいごとで終わらせない。
- 一人称は「僕」。読者への呼びかけは名前＋さん（メルマガでは `%name1%さん`）。

## 原稿を出すときの形式

生成した原稿は、そのままコピペして配信できる状態で出すこと。
解説やメタコメントを本文に混ぜない。必要な補足は本文の外に書く。

## サムネを作るとき

1. `templates/thumbnail-copy.md` の型でコピー案を出す
2. `tools/README.md` の書式で spec.json を書く
3. `node tools/thumbnail.mjs <spec.json> <出力先.png>` で生成する

イラストや写真は生成できません。文字組みのサムネと、用意した画像への文字入れが対象です。
